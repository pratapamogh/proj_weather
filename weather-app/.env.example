# WeatherSphere Environment Variables

# The Open-Meteo API doesn't require an API key, but if we migrate 
# to a different provider in the future (like OpenWeatherMap), 
# place the keys here.
VITE_WEATHER_API_KEY=
VITE_GEO_API_KEY=
